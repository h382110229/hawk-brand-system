name: UI Foundation CI
on:
  push:
    branches: [main, feat/*]
  pull_request:
    branches: [main]

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 11
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm tokens:check
      - run: pnpm tokens:build
      - run: pnpm brand:sync
      - run: pnpm lint
      - run: pnpm test
      - run: pnpm build
      - name: Install Chrome
        id: setup-chrome
        uses: browser-actions/setup-chrome@v1
        with:
          chrome-version: stable
      - name: Foundation browser verification
        run: pnpm browser:verify
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: foundation-screenshots
          path: deliverables/ui-foundation-01/actual/
          retention-days: 30
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: foundation-verification-report
          path: deliverables/ui-foundation-01/actual/verification-report.json
          retention-days: 30
      - name: Component-02 browser verification
        run: pnpm component-02:browser-verify
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: component-02-screenshots
          path: deliverables/ui-component-02/actual/
          retention-days: 30
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: component-02-verification-report
          path: deliverables/ui-component-02/actual/verification-report.json
          retention-days: 30
      - name: Component-02B browser verification
        run: pnpm component-02b:browser-verify
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: component-02b-screenshots
          path: deliverables/ui-component-02b-input/actual/
          retention-days: 30
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: component-02b-verification-report
          path: deliverables/ui-component-02b-input/actual/verification-report.json
          retention-days: 30
      - name: Component-03 browser verification
        run: pnpm component-03:browser-verify
        env:
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: component-03-screenshots
          path: deliverables/ui-component-03-card/actual/
          retention-days: 30
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: component-03-verification-report
          path: deliverables/ui-component-03-card/actual/verification-report.json
          retention-days: 30
